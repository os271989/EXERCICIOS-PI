#include <stdio.h>
#include "Funcoes.h"

int calcSoma(int x, int y);
float calcMedia(float x, int y);

int calcSoma(int x, int y)
{
	return(x + y);
}

float calcMedia(float x, int y)
{
	return (x / y);
}